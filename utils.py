# In here I have defined a utility function format_minutes that takes a number of minutes as input and returns a formatted string.
def format_minutes(minutes):
    """Return a formatted string for the interval."""
    return f"{minutes} minute(s)"